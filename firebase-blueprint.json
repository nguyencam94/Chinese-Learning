{
  "entities": {
    "Vocabulary": {
      "title": "Vocabulary",
      "description": "A specific word or phrase saved by the user from their sentences",
      "type": "object",
      "properties": {
        "word": { "type": "string", "description": "The Chinese word or phrase" },
        "type": { "type": "string", "enum": ["word", "grammar"], "description": "The category of the saved item" },
        "userId": { "type": "string", "description": "The ID of the user" },
        "sentenceId": { "type": "string", "description": "The ID of the sentence it was saved from" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp" }
      },
      "required": ["word", "type", "userId", "sentenceId", "createdAt"]
    },
    "Category": {
      "title": "Category",
      "description": "A user-defined category for organizing saved sentences",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the category" },
        "userId": { "type": "string", "description": "The ID of the user who owns the category" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation" }
      },
      "required": ["name", "userId", "createdAt"]
    },
    "SavedSentence": {
      "title": "Saved Sentence",
      "description": "A sentence translated and saved by the user for learning",
      "type": "object",
      "properties": {
        "originalText": { "type": "string", "description": "The Vietnamese text input by the user" },
        "chinese": { "type": "string", "description": "The translated Chinese characters" },
        "pinyin": { "type": "string", "description": "The Pinyin pronunciation" },
        "meaning": { "type": "string", "description": "The English meaning" },
        "grammarExplanation": { "type": "string", "description": "Markdown explanation of the grammar" },
        "userId": { "type": "string", "description": "The ID of the user who saved the sentence" },
        "categoryId": { "type": "string", "description": "The ID of the category this sentence belongs to" },
        "sectionId": { "type": "string", "description": "The ID of the section this sentence belongs to" },
        "orderIndex": { "type": "number", "description": "Ordinal position index of the sentence within its section (1 for topic sentence, 2, 3...)" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the sentence was saved" },
        "note": { "type": "string", "description": "Optional study note or sentence pattern explanation written in Vietnamese" },
        "difficulty": { "type": "string", "enum": ["basic", "easy", "medium", "hard"], "description": "Classification level of difficulty of the sentence" },
        "illustrationSvg": { "type": "string", "description": "A beautiful vector HTML/SVG inline illustration representing the sentence" },
        "variations": { "type": "array", "description": "Alternative variations of the sentence generated by Gemini" }
      },
      "required": ["originalText", "chinese", "pinyin", "meaning", "grammarExplanation", "userId", "createdAt"]
    },
    "Section": {
      "title": "Section",
      "description": "A subdivision within a category (e.g., a conversation or paragraph)",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the section" },
        "categoryId": { "type": "string", "description": "The ID of the parent category" },
        "userId": { "type": "string", "description": "The ID of the user who owns the section" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation" }
      },
      "required": ["name", "categoryId", "userId", "createdAt"]
    },
    "StudySession": {
      "title": "Study Session",
      "description": "Daily study session tracking for a user",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The ID of the user" },
        "date": { "type": "string", "description": "YYYY-MM-DD local date of study" },
        "duration": { "type": "integer", "description": "Total study duration in seconds" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Timestamp of last update" }
      },
      "required": ["userId", "date", "duration", "updatedAt"]
    },
    "SavedCharacter": {
      "title": "Saved Character",
      "description": "A single Chinese character analyzed by AI and saved by user with radicals and examples",
      "type": "object",
      "properties": {
        "character": { "type": "string", "description": "The Chinese character" },
        "pinyin": { "type": "string", "description": "Pinyin pronunciation" },
        "sinoVietnamese": { "type": "string", "description": "Sino-Vietnamese reading" },
        "vietnameseMeaning": { "type": "string", "description": "Core Vietnamese meaning" },
        "totalStrokes": { "type": "integer", "description": "Total number of strokes" },
        "strokeSequenceInstructions": { "type": "array", "description": "Step-by-step stroke instructions" },
        "radicals": { "type": "array", "description": "Radicals composing the character" },
        "composition": { "type": "string", "description": "Character composition explanation" },
        "examples": { "type": "array", "description": "Compound words containing this character" },
        "userId": { "type": "string", "description": "The ID of the user" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation" },
        "note": { "type": "string", "description": "Optional study note" }
      },
      "required": ["character", "pinyin", "sinoVietnamese", "vietnameseMeaning", "totalStrokes", "radicals", "composition", "examples", "userId", "createdAt"]
    }
  },
  "firestore": {
    "/saved_sentences/{sentenceId}": {
      "schema": "SavedSentence",
      "description": "Collection of sentences saved by users"
    },
    "/categories/{categoryId}": {
      "schema": "Category",
      "description": "Collection of user-defined categories"
    },
    "/sections/{sectionId}": {
      "schema": "Section",
      "description": "Collection of user-defined sections within categories"
    },
    "/vocabulary/{wordId}": {
      "schema": "Vocabulary",
      "description": "Collection of saved words/vocabulary"
    },
    "/study_sessions/{sessionId}": {
      "schema": "StudySession",
      "description": "Daily study duration logs"
    },
    "/saved_characters/{characterId}": {
      "schema": "SavedCharacter",
      "description": "Collection of saved Chinese characters with radicals and vocabulary examples"
    }
  }
}
